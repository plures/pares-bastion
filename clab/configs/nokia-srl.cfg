// Nokia SR Linux minimal config for fixture recording
// Enables SSH, SNMP, sets hostname and management interface
set / system hostname nokia-srl-lab
set / system snmp community public authorization read-only
set / system ssh-server admin-state enable
set / system lldp admin-state enable
